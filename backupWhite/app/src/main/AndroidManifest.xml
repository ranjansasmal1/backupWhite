<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.playmusic">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <permission android:name="android.permission.MEDIA_CONTENT_CONTROL" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".SplashScreen"
            android:label="@string/app_name"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".HomeActivity"
            android:theme="@style/AppTheme.NoActionBar"/>

        <receiver android:name=".receiver.NextBroadcastReceiver">
            <intent-filter>
                <action android:name="com.playmusic.NEXT_ACTION"/>
            </intent-filter>
        </receiver>
        <receiver android:name=".receiver.PreviousBroadcastReceiver">
            <intent-filter>
                <action android:name="com.playmusic.PREVIOUS_ACTION"/>
            </intent-filter>
        </receiver>
        <receiver android:name=".receiver.PlayPauseBroadcastReceiver">
            <intent-filter >
                <action android:name="com.playmusic.PLAY_PAUSE_ACTION"/>
            </intent-filter>
        </receiver>
        <receiver android:name=".receiver.StopServiceBroadcastReceiver">
            <intent-filter>
                <action android:name="com.playmusic.STOP_SERVICE"/>
            </intent-filter>
        </receiver>
    </application>

</manifest>